# プロジェクト規約：タスク管理ツール（Trello風）

このファイルは Claude Code が毎回読み込む指示書です。
ここに書かれたルールは**例外なく守ること**。判断に迷ったら、勝手に進めず利用者に確認する。

---

## 1. Git / GitHub の進め方（最重要）

### 1.1 絶対に守ること

- **`main` ブランチで直接作業しない。** `main` へのコミット・プッシュは禁止。
- **作業を始める前に、必ず GitHub の Issue を作る。** Issue のない作業を始めてはいけない。
- **`main` への反映は必ず Pull Request 経由で行う。**
- 上記に反する操作（`main` で `git commit`、`git push origin main` など）を求められた場合は、
  実行せずにこのルールを示し、Issue とブランチを作る手順を提案する。

### 1.2 作業の流れ

1. **Issue を作る** — 何を、なぜ作るのかを書く（テンプレートは 1.4）
2. **`main` を最新にする** — `git switch main && git pull`
3. **ブランチを切る** — 命名規則は 1.3
4. **実装する** — こまめにコミット（コミット規約は 1.5）
5. **テストを通す** — `backend/` を触ったら `./gradlew test`、`frontend/` を触ったら `npm run build`
6. **プッシュして PR を作る** — 本文に `Closes #<Issue番号>` を必ず書く
7. **マージ後** — `main` を pull し、作業ブランチを削除する

### 1.3 ブランチの命名規則

```
<種別>/<Issue番号>-<内容を英語の小文字とハイフンで>
```

| 種別 | 使う場面 | 例 |
|---|---|---|
| `feature` | 機能の追加 | `feature/12-board-api` |
| `fix` | 不具合の修正 | `fix/15-login-401` |
| `docs` | ドキュメントのみの変更 | `docs/18-update-api-design` |
| `chore` | 設定・依存関係・雑務 | `chore/20-add-ci` |
| `refactor` | 動きを変えない内部改善 | `refactor/22-extract-service` |

- ブランチは必ず最新の `main` から切る
- 1ブランチ＝1 Issue。複数の Issue をまとめない

### 1.4 Issue の書き方

タイトルは日本語で、何をするのかが一目で分かるように書く（例：「ボード API を実装する」）。
本文には次の3つを必ず含める。

```markdown
## 目的
なぜこれをやるのか。どの要件・設計書に対応するか（例：F-11〜F-14、04 API設計書 3.2）

## やること
- [ ] 具体的な作業を箇条書きで
- [ ] 完了の判断ができる粒度にする

## 完了の条件
どうなったら終わりと言えるか（例：Swagger UI から4つの API が動き、テストが通る）
```

ラベルは `backend` / `frontend` / `docs` / `infra` から当てはまるものを付ける。

### 1.5 コミットの規約

- **メッセージは日本語**で書く（ドキュメントと揃えるため）
- 1行目は50文字程度の要約。末尾に句点は付けない
- 2行目は空行。3行目以降に「なぜそうしたか」を書く（何をしたかはコードを見れば分かる）
- 末尾に必ず次の1行を入れる

```
Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
```

- **テストが通らない状態でコミットしない**
- 生成物（`build/`、`node_modules/`、`dist/`）をコミットしない

### 1.6 Pull Request の書き方

```markdown
## 概要
この PR で何が変わるのか、2〜3行で

## 変更内容
- 主な変更を箇条書きで

## 動作確認
実際に確認した内容（テストの結果、Swagger UI での確認結果など）

Closes #<Issue番号>
```

- 末尾に次の1行を入れる

```
🤖 Generated with [Claude Code](https://claude.com/claude-code)
```

- マージ方法は **Merge commit**（履歴に作業の流れを残すため）

---

## 2. 設計書との付き合い方

`docs/` のドキュメントが**仕様の正**。コードが設計書と食い違ったら、どちらが正しいかを利用者に確認する。

- 実装で設計と違う判断をしたら、**該当する設計書を必ず更新し、改訂履歴に1行追加する**
- 設計書の更新はコードと同じ PR に含める（後回しにしない）
- ドキュメントは「お客様に説明する資料」という想定で、専門用語には説明を添えて日本語で書く

| No | ドキュメント | 役割 |
|---|---|---|
| 01〜01-5 | 要件定義書ほか | 何を作るか。機能ID（F-xx）・業務ルールの出典 |
| 02 | 技術選定書 | 使用技術と、選んだ理由・選ばなかった理由 |
| 03 | DB設計書 | テーブル定義と DDL。Flyway の SQL はここから写す |
| 04 | API設計書 | エンドポイント・JSON・エラー形式の正 |
| 05 | 画面設計書 | 画面項目・画面遷移・メッセージ文言の正 |

---

## 3. コードを書くときの約束

### 3.1 共通

- **コメントとドキュメントは日本語**。変数名・クラス名は英語
- 設計書の該当箇所をコメントで示す（例：`// 業務ルール 5.6`）
- 利用者は学習中のため、**「なぜそう書くのか」が分かるコメント**を残す

### 3.2 バックエンド（`backend/`）

- 層は Controller → Service → Repository。Controller に業務ルールを書かない
- **すべての Service で「そのデータがログイン中の利用者のものか」を確認する**。
  他人のデータのときは 403 ではなく **404** を返す（存在を知らせないため）
- 入力チェックは Bean Validation で行い、メッセージは画面にそのまま出せる日本語にする
- エラーは ProblemDetail（RFC 9457）形式で返す
- Lombok は使わない。DTO は record で書く
- 新しい機能には必ずテストを書く（Service のユニットテスト＋API の結合テスト）

### 3.3 フロントエンド（`frontend/`）

- サーバーのデータは TanStack Query で扱う。独自の状態管理ライブラリは入れない
- `fetch` には必ず `credentials: 'include'` を付ける（付けないと常に 401 になる）
- GET 以外の通信には `X-XSRF-TOKEN` ヘッダーを付ける（付け忘れると 403 になる）
- 画面の文言は [05 画面設計書](docs/05_screen-design.md) 8章のメッセージ一覧に合わせる

---

## 4. 環境まわりの注意（Windows）

- **ブランチを切り替える前に `cd backend && ./gradlew --stop` を実行する。**
  Gradle デーモンが `gradle-wrapper.jar` を掴んだままだと、切り替えが途中で失敗して
  作業ツリーが壊れる
- Java は Temurin 21 を使う。`JAVA_HOME` はユーザー環境変数に設定済み
- テストと開発には Docker Desktop の起動が必要（Testcontainers が PostgreSQL を起動するため）
- 開発用 DB の起動：リポジトリ直下で `docker compose up -d`
