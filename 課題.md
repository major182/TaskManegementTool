# 🌐 Webアプリケーション開発 ITリテラシー理解度チェック

タスク管理アプリ **「TaskManagement」** の開発を通じて身につけるべき、

# 第1章 インターネットとネットワークの仕組み（12問）

## Q1. インターネットとは

「インターネット」と「Webブラウザで見るホームページ」は同じものですか？違うとすれば、どう違いますか？

A. インターネットは端末同士を繋げる通信インフラのことで、ホームページはインターネットを利用したサービスのひとつ。

---

## Q2. IPアドレスとポート番号

このプロジェクトではフロントエンドを `localhost:5173`、バックエンドを `localhost:8080` で起動しています。

- `localhost`とは何を指しますか？
- `:5173` や `:8080` のようなポート番号はなぜ必要ですか？

A.　Localhostは自分の使っているPCのIPアドレスを指す名称 
　　もしもポート番号という仕組みがなかったら、一つのIPアドレスに対して行われた通信が、
　　IPアドレス内のどのアプリ宛の通信なのか判別ができず、通信を成立させるためには、
　　一つのアプリしか使用できなくなる。ポート番号があることで、どのアプリ宛の通信かを判別でき、
　　2つ以上のアプリが同時に通信することが出来ている。

---
## Q3. DNS

ブラウザのアドレスバーに
`https://www.example.com`
と入力してからページが表示されるまでに、DNSはどのような役割を果たしますか？

A.　URLと紐づくIPアドレスに置換してくれている。
　　例えるなら、「電話帳で名前検索をかけ、紐づく電話番号を見つける」この作業をDNSが担っている。

---
## Q4. HTTPとは

HTTPとは何の略で、どのような役割を持つものですか？
「プロトコル」という言葉の意味も含めて説明してください。

A.　プロトコルは、相互に通信を行う際のルールであり、
　　HTTPは、そのプロトコルのうち、「Webブラウザ、サーバー間」でデータを通信するためのルールである。

---



## Q5. HTTPとHTTPSの違い

HTTPとHTTPSの違いは何ですか？
なぜ現在のWebサイトはほぼすべてHTTPSを使用していますか？

A.　Webブラウザとサーバー間の通信が暗号化されているかどうかの違いで、
　　HTTPだと個人情報を入力するようなWebサイトでは、通信中のデータが見えてしまうことになるため、
　　セキュリティ対策の観点からHTTPSが一般的になっている。

---



## Q6. リクエストとレスポンス

Webにおける「リクエスト」と「レスポンス」の関係を、日常生活の例えを使って説明してください。

A.　コンビニで客(Webブラウザ)が商品を注文(リクエスト)し、
　　店員(サーバー)が商品を提供(レスポンス)する。

---



## Q7. HTTPメソッド

このアプリのAPIでは以下を使っています。

- GET
- POST
- PUT
- DELETE
- PATCH

これらの「HTTPメソッド」とは何ですか？
日常生活の例えで、それぞれの違いを説明してください。

A.　HTTPメソッドはサーバーに対する命令の種類であり、サーバーをメモ帳とすると、
　　GET     メモ帳を開いて内容を見る
　　POST    メモ帳の新しいページに内容を書く
　　PUT     メモ帳を一枚破り捨て、同じページに新たなメモを追加する
　　DELETE  メモ帳を一枚破り捨てる
　　PATCH   メモ帳の内容の一部を書き換える

---



## Q8. HTTPステータスコード

サーバーからの返事には「ステータスコード」という3桁の数字が含まれます。

以下のコードはそれぞれ何を意味しますか？

- 200
- 201
- 400
- 404
- 500

A.　200 (OK)    リクエストどおりのデータを返せた
　　201 (Created)   新しいデータが登録された
　　400 (BadRequest)    リクエスト失敗
　　404 (NotFound)  指定されたデータが存在しない
　　500 (Error) サーバー内部でエラーが発生して処理出来ない

---



## Q9. JSONとは

フロントエンドとバックエンド間でデータをやり取りする際に「JSON」という形式が使われています。

- JSONとは何ですか？
- なぜ広く使われていますか？

A.　JSONはデータ構造(名前＝◯◯のようなキーと値の組み合わせ)をテキストで表現するためのフォーマットで、
　　フロントエンドとバックエンドは違う言語で動いているが、JSONという共通のフォーマットがあるおかげで、
　　データのやり取りが出来ている。

　　普及している理由としては、どのプログラミング言語でも使用でき、テキスト形式なので可読性も高いため。

---



## Q10. APIとは

このアプリではフロントエンドがバックエンドのAPIを呼び出してデータを取得しています。

- APIとは何ですか？
- なぜ直接データベースにアクセスしないのですか？

A.　APIとはアプリ同士がデータ連携するための窓口機能である。
　　アプリが持っているDBを他アプリが直接見れてしまわないように、窓口を設け、
　　必要なデータだけを受け渡すように設計されている。

　　他アプリがDBを直接見れてしまうということは、個人情報やパスワードの漏洩のリスクがあるため。

---



## Q11. CORSとは

フロントエンド（5173）からバックエンド（8080）にアクセスする際、

「CORS」という仕組みが関係します。
CORSとは何のためにありますか？

A.　Webブラウザにはセキュリティの大原則として、SOP(Same-OriginPolicy)という安全装置があります。
　　これは「あるWebサイトから実行されたプログラムは、同じ場所にあるサーバーとしか通信できない」
　　という制限です。
　　この制限があることによって、ブラウザで悪意のあるWebサイトを開いた時、ブラウザに保存されている
　　ログイン情報を使って別サーバーへ通信され、情報が漏洩することを防いでいます。
　　
　　しかしSOPがあると、フロントエンドとバックエンドで場所が異なる場合には通信ができなくなります。
　　そこでCORS(Cross-Origin ResourceSharing)という仕組みが必要になり、これはSOPに例外を設け、
　　違う場所にあるサーバーでも限定的に通信を許可する仕組みです。

---



## Q12. クライアントとサーバー

「クライアント」と「サーバー」とは何ですか？
このアプリの構成の中で、それぞれどれがクライアントでどれがサーバーですか？

- ブラウザ
- バックエンド
- データベース

A.　要求する側がクライアント、提供する側がサーバーであり、1対1の通信における「相対的な役割」を表す
　　上記の場合、ブラウザ⇔バックエンド、バックエンド⇔DB、という相対関係が考えられるので、
　　「ブラウザ」はクライアント、「DB」サーバー、「バックエンド」はクライアントとサーバーの両方となる。

---



# 第2章 Webアプリケーションの構造（8問）



## Q13. フロントエンドとバックエンド

「フロントエンド」と「バックエンド」の違いを説明してください。
このアプリではそれぞれ何を担当していますか？

A.　フロントエンドは、画面の見た目や状態を管理する機能。
　　バックエンドは、見えない裏側でサーバーとの通信や認証などを行う機能。

　　タスク管理アプリのログイン画面を例にすると、
　　フロントエンドとして、ログインの画面を表示し、ユーザーがID等を入力する。
　　バックエンドでDBにアクセスし、IDが存在していることを確認した。

---



## Q14. 3層構造

このアプリは

- フロントエンド
- バックエンド
- データベース

の3層構造です。

なぜこのように役割を分けるのですか？
1つのプログラムですべてをやらない理由を説明してください。

A.　修正が必要になった時に該当の役割のソースだけを見ることが出来る（保守性）

　　すべてが一つのプログラムだとDBが公開されることになるため、
　　分けることによってDBを公開しない作りが可能になる（セキュリティ）

　　例えば、「似たような機能を持つ、見た目の違う別のアプリ」を開発する際に、
　　バックエンドとDBはそのまま使い回せる（拡張性）

---



## Q15. データの流れ

ユーザーが「新しいタスクを作成」ボタンを押してから、

- データベースに保存される
- 画面に反映される

までの流れを順を追って説明してください。

A.　「新しいタスクを作成」ボタンを押すと、①(フロントが入力画面を表示)し、ユーザーが入力すると、
　　②（フロントがタスクを画面に反映※1）すると同時に③（フロントがバックに対し登録の指示）を行う。
　　④（バックが入力チェックと権限確認を行い、DBに登録処理）がされ、データが保存される。
　　DB保存されたデータがバックを通してフロントへ渡され、⑤（フロントが本物のデータで画面に反映）する。
　　
　　※1　今回作成したアプリでは、楽観的更新処理を採用しているため、④の処理でエラーとなった場合は、
　　　②の処理をもとに戻して、エラーで登録できなかった旨を知らせる。

---



## Q16. エンドポイントとURL設計

このアプリには以下のAPIがあります。

- `/api/tasks`
- `/api/tasks/{id}`

このようなURL設計の考え方を説明してください。
`{id}` の部分は何ですか？

A.　URLはモノの住所を示し、何をするかはHTTPメソッドで表す。この2つでエンドポイント1つとし、
　　このようにルール化する事で、どこのデータ(URL)を操作(HTTPメソッド)するという組み合わせで表現ができ、
　　機能が増えるごとにURLが増大することを避けることが出来る。
　　{id}の部分はパスパラメータと言い、tasksの集まりの中の特定のデータを指定できる。

---



## Q17. 同期処理と非同期処理

「同期処理」と「非同期処理」の違いを、日常生活の例えで説明してください。
なぜWeb通信では非同期処理が必要ですか？

A.　日常生活で例えると、ご飯を食べる際に、店内で席に座ってご飯を待つのが「同期処理」、
　　出前を取り、自宅で待つのが「非同期処理」
　　お店だとご飯を待っている間、洗濯とか入浴とかは出来ないが、出前なら出来る。

　　Web通信において、JavaScriptなどはシングルスレッドと言って、同時に持てる処理の流れが1つだけです。
　　「画面の描画」「スクロール」「ボタン処理」もすべてこの一本の流れで行っているため、
　　すべてが同期処理だった場合、一つの処理でAPIの通信を待っている間、画面の更新もされず、
　　スクロールもボタンの操作もできなくなります。
　　1つの処理が終わるまで他の操作を開始できない状態では使い物にならないため、Web通信においては
　　非同期処理が採用されています。

---



## Q18. SPA（シングルページアプリケーション）

このアプリはReactで作られたSPAです。
従来のWebサイト（ページ遷移するもの）と何が違いますか？

A.　SPAは画面が読み込むHTMLが一つだけの構成で作られたアプリのことで、
　　HTMLを読み込んだ後は必要なデータのみを取得して、部分的に描画を書き換える処理をします。

　　従来のページ遷移するWebサイトでは、リンクを押すたびに、新しいHTMLを丸ごと読み込む仕組みです。

---



## Q19. 入力チェック（バリデーション）の多重防御

入力チェックはフロントエンドとバックエンドの両方で行うべきと言われています。
なぜ片方だけでは不十分なのですか？

A.　フロントの動作はブラウザの開発者ツールなどから入力チェックを無効に
　　することが出来てしまう。そのためフロントだけでは安全性が担保できない。
　　バックの動作は利用者が触れないため、チェックを確実に行うことが出来るが、
　　フロントで入力後、バックへ通信を行い、バックでチェックを実行し、フロントへ結果を返すため、
　　内容が有効でも無効でも時間がかかる。そのため、安全性は担保できるが、使用感が悪い。

　　従って、フロントとバックでそれぞれチェックを設けることで、安全性と使用感を補い合っている。

---



## Q20. エラーハンドリング

API呼び出しが失敗した場合（ネットワーク障害やサーバーエラーなど）、
アプリはどのように対処すべきですか？

A.　まずは、失敗する前の状態に画面を戻し、ユーザーへ失敗した内容を通知する。
　　次に、やり直す手段を提示する、例えばログイン画面に戻すことや、再試行というボタンを出すなど。

---



# 第3章 データベースの基礎（8問）



## Q21. データベースとは

データベースとは何ですか？
ExcelやCSVで保存する方法と比べて何が優れていますか？

A.　データベースとは、大量のデータを決まった形式で整理して保存し、
　　必要なデータを素早く取り出せるようにした仕組みである。

　　EXCELやCSVと比較した時に、データベースは読み書きや検索する速度が圧倒的に早く、
　　複数の同時アクセスが可能。また、データの形式を固定出来るため、一貫性を保つことが出来る。

---



## Q22. テーブル・行・列

以下をExcelのシートに例えて説明してください。

- テーブル
- 行（レコード）
- 列（カラム）

tasksテーブルで具体例も挙げてください。

A.　テーブルはEXCELのシートで、列はシートの見出しにある項目、
　　行は横1行でデータ一つ分となる。(当アプリではカード一枚分)

　　当アプリではtasksテーブルの代わりに、cardsテーブル(タスク管理アプリのカード機能)があり
　　列には、list_id, title, descriptionなどがあり、見出しの役割をしている。
　　行には、列に対応する値が入っており、
　　例えばtitleにはユーザーが入力したカードのタイトルが文字列として保存されている。
　　
---



## Q23. 主キー（PRIMARY KEY）

tasksテーブルの `id` は主キーです。

- 主キーとは何ですか？
- なぜ必要ですか？

A.　主キーとはDBの中で1件を識別するための列であり、重複や空欄が許されない性質を持つため、
　　1行のデータを固有のものとして管理できる。
　　主キーが無いと、固有のデータを直接指定する手段がなくなってしまう。例えば、
　　同じタイトルのカードが2枚あるとタイトルでは区別できず、更新や削除の対象を1件に絞れない。

---



## Q24. CRUD操作

CRUDとは何の略ですか？
それぞれがこのアプリのどの機能に対応するか説明してください。

- Create
- Read
- Update
- Delete

A.　CRUDとは下記4つのデータに対して出来るアクションの、アルファベットの頭文字である。
　　Create  「リストやカードの新規作成」処理などに使用
　　Read    「リストやカードを表示する」処理などに使用
　　Update  「カードのタイトルを更新」処理などに使用
　　Delete  「リストやカードの完全削除」処理に使用。
　　　　※本アプリではボードからの削除でゴミ箱へ、(テーブルのdeleted_atデータのUpdate)
　　　　　ゴミ箱からの削除でDBからの削除としている。(Delete)

---



## Q25. データの制約

tasksテーブルでは

- `NOT NULL`
- `DEFAULT 'medium'`

が設定されています。
これらは何のためにありますか？

A.　`NOT NULL`は、その列を空欄（NULL）のままでは保存させないという制約である。
　　`DEFAULT 'medium'`は、値を指定せずに保存した時に自動で入る初期値である。
　　　この場合はmediumが初期値として保存される。

---



## Q26. インデックス

インデックスとは何ですか？
本の索引に例えて説明してください。

A.　データベースにおけるインデックスとは、ある列の値から該当する行の場所を素早く検索できるように、
　　値を並べ替えて別に用意しておく仕組みのことである。

　　本の索引があれば、索引から知りたい単語を検索し、素早くそのページにたどり着けるが、
　　もし索引がなければ、本を頭からすべて読んで探さなくてはならない。

---



## Q27. トランザクション

トランザクションとは何ですか？
タスクを別カラムへドラッグ移動する操作で、なぜ重要ですか？

A.　データベースのトランザクションとは、複数の変更を1つのまとまりとして扱い、
　　「全部成功」か「全部なかったこと」のどちらかにする仕組みである。

　　カードを別のリストへ移動する操作は、画面上はD&Dの操作1回であるが、DBへの変更は複数回発生する。
　　具体的には、以下3つの処理が起る。
　　　①移動元リストから抜いて残ったカードの順番を整理し
　　　②カードの位置を移動先リストに書き換え
　　　③移動先リスト順番も整理する

　　もしトランザクションがなく途中で失敗すると、カードが移動元からも移動先からも消えてデータが失われる。
　　また、順番の振り直しに失敗すると順番が重複し、データベースの制約に違反して、
　　そのリストが操作不能になる、といった状態が起こってしまう。
　　
　　トランザクションがあればどの段階で失敗しても移動前の状態に丸ごと戻るため、
　　データが壊れることを防げ、ユーザーには移動できないエラーを通知するだけで済む。

---



## Q28. SQLとORM

- SQLとはどのようなものですか？
- ORMの役割とは何ですか？

A.　SQLとはデータベースを操作するための言語である。Q24で出たCRUDに対応する指示文があり、
　　処理の内容を記述するのではなく、データの条件のみを記述するのが特徴である。

　　ORMはSQLでの指示を、プログラムの「クラス」や「インスタンス」などに紐づけることで、
　　SQLを書かずにDBを操作できるようにする道具である。
　　ORMがない場合、SQLでの指示文を文字列として出力し、
　　返ってきた結果をプログラム内で変数へ収める工程が発生する。
　　この工程を省略出来るほか、DB製品を変えても、方言の差をORMが吸収してくれる。

---



# 第4章 開発プロセスとチーム開発（4問）



## Q29. バージョン管理（Git）

Gitなどのバージョン管理システムとは何ですか？
なぜ必要ですか？
使わないとどんな問題が起きますか？

A.　バージョン管理システムとは、ファイルの変更履歴（差分）を記録し、
　　変更点を見比べることが出来るほか、過去の状態に戻すことも出来る仕組みです。

　　「プログラムを数カ所変更して保存した際に、プログラムがエラーで動かなくなった。」
　　と言った場合、変更箇所の差分が分かればもとに戻すことも、修正することも出来ます。
　　バージョン管理をしていない場合、プログラムをもとに戻そうと操作したつもりが、
　　別のところを変更してしまい、余計にエラーが増える。といった事態に陥りかねません。
　　また、複数人での開発では、1つのファイルを同時に編集した場合、後から保存したものが正となり、
　　前に保存したデータは上書きされて消えてしまいますが、バージョン管理システムを用いれば、
　　両方の変更を保存でき、競合した箇所をハイライトで知らせることが出来るため、競合の解決も容易です。

　　安定した開発環境には必須といえるシステムです。

---



## Q30. ブランチとPull Request

- ブランチとは何ですか？
- Pull Request の目的は何ですか？
- なぜ全員が main に直接変更しないのですか？

A.　ブランチとはGitなどのバージョン管理システムで、作業領域を切り替える手段である。
　　mainブランチとは常に別のブランチで開発や修正を行うことで、バグなどが発生しても、
　　mainブランチを常に動作する完成版として維持することができる。
　　また、機能ごとにブランチを分ければ複数人が並行して作業できる。

　　PullRequestとは、ブランチの変更をmainに取り込む前に、レビューを行うための仕組みである。
　　変更差分が一覧で表示されるため、mainにバグや仕様にない事を取り込まないようチェックできる。

　　複数人開発においてmainに直接変更しない理由としては、もしmainがバグで壊れてしまった時、
　　mainをプルした途端に、自分の作業によるバグなのか、他人の作業によるバグなのか判断できなくなる。
　　また、mainは本番環境にリリース出来る状態を維持するために、常に動く状態で保存する運用をしたいため、
　　直接変更を加えることがそもそものリスクとなる。

---



## Q31. コミットメッセージの重要性

例：

`feat: ログイン機能を追加`

良いコミットメッセージを書くことがなぜ重要ですか？

A.　バージョン管理システムで、過去の変更履歴を追う際に、適切なコメントが書かれていないと、
　　変更箇所を全て確認し、何が変わった変更なのかを読み解く必要がある。
　　対して、適切なコメントが書かれていると、変更の全貌を理解したうえで、
　　変更箇所を見に行くことが出来るため、全文を読むという工程を短縮できる。
　　また、なぜその変更を採用したのか。という情報はコードには残らないため、
　　ドキュメントやコメントが重要になる。

---



## Q32. なぜ技術的な仕組みを理解すべきか

AI時代においてコードはAIが書けるようになりました。

それでも

- ネットワーク
- データベース
- Webアプリケーションの仕組み

を理解する必要がある理由を、あなたの考えで述べてください。

A.　一つはAIの成果物に対して、良し悪しを判断出来なくなるためである。
　　もう一つはAIに指示を出すうえで、仕様を決められないためである。
　　
　　AIは効率的に作業が出来るため、「フロントエンドとバックエンドを分けて実装する」など、
　　人が積み上げてきた安全な設計を踏襲せずに作ることが出来てしまう。
　　指示する側である人がこのような知識を欠いていると、動作はするが、安全性に欠けていたり、
　　デファクトスタンダードから外れたプログラムが成果物として出来てしまう可能性がある。
　　また、設計や仕様は人が決めるものであり、AIに機能要件が曖昧な指示をすると、
　　技術レベルで何を採用するかを聞き返したり、確率の高い候補を勝手に決定したりしてしまう。
　　このように、しっかりと仕組みを理解する事で、初めてAIをマネジメントする事が出来るためである。

　　